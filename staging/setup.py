from setuptools import find_packages
from setuptools import setup

REQUIRED_PACKAGES = []

setup(
    name='sg',
    version=0.1,
    install_requires=REQUIRED_PACKAGES,
    include_package_data=True,
    description='sample trainer package'
)